#
# App-specific constants and defaults.
#

app:
  name: 'Teamwork'
  rootElement: 'root'

devtools: true

client:
  keyStorage:
    root: "dxos/teamwork/keys"

  feedStorage:
    root: "dxos/teamwork/feeds"

  swarm:
    signal: "wss://demo.kube.dxos.network/dxos/signal"
    ice:
      - urls: "stun:stun.wirelne.ninja:3478"
      - urls: "turn:stun.wireline.ninja:3478"
        username: "wireline"
        credential: "wireline"

debug:
    mode: 'development'
