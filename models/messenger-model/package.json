{
  "name": "@dxos/messenger-model",
  "version": "1.2.0-alpha.65",
  "private": false,
  "license": "MIT",
  "main": "dist/src/index.js",
  "files": [
    "src",
    "src/proto/gen",
    "dist"
  ],
  "scripts": {
    "build": "yarn build:protobuf && tsc --build",
    "build:protobuf": "mkdir -p src/proto/gen && build-protobuf src/proto/defs/messenger.proto -o src/proto/gen"
  },
  "dependencies": {
    "@dxos/codec-protobuf": "~1.1.1",
    "@dxos/util": "~2.6.22",
    "debug": "^4.2.0"
  },
  "devDependencies": {
    "@dxos/protobuf-compiler": "~1.1.1",
    "typescript": "^4.0.3"
  },
  "peerDependencies": {
    "@dxos/model-factory": "*"
  },
  "publishConfig": {
    "access": "public"
  }
}
